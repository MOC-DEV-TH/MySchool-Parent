import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AboutMySchool = (props) => {
  return (
    <View>
    </View>
  );
};

export default AboutMySchool;

const styles = StyleSheet.create({});
