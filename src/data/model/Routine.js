class Routine {
  constructor(name, duration, subject, isActive) {
    this.name = name;
    this.duration = duration;
    this.subject = subject;
    this.isActive = isActive;
  }
}

export default Routine;
